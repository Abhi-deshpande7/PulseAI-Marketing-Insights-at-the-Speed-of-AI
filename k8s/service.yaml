apiVersion: v1
kind: Service
metadata:
  name: pulseai-service
  namespace: pulseai
spec:
  selector:
    app: pulseai
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8501
  type: NodePort
